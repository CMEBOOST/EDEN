# คัดลอกเป็น .env แล้วแก้ค่า
# base URL ของ backend API — Vite inline ตอน build / อ่านตอน dev server
# ไม่ตั้ง = fallback http://localhost:8000 (พอสำหรับ local dev)
VITE_API_BASE=http://localhost:8000
